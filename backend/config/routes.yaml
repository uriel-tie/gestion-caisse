controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

# Ajout OBLIGATOIRE pour que le bundle JWT intercepte la connexion
api_login_check:
    path: /api/login_check